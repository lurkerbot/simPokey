{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "lldb-mi",
      "request": "launch",
      "target": "${workspaceRoot}/src/pokey",
      "cwd": "${workspaceRoot}/src",
      "preLaunchTask": "build-all"       
    }
  ]
}